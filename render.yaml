services:
  - type: web
    name: coachline-api
    runtime: node
    region: oregon
    plan: free
    buildCommand: npm ci && npx prisma generate && cd apps/api && npm run build
    startCommand: cd apps/api && npm start
    healthCheckPath: /health
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: coachline-db
          property: connectionString
      - key: REDIS_URL
        fromService:
          name: coachline-redis
          type: redis
          property: connectionString
      - key: NODE_ENV
        value: production
      - key: PORT
        value: "3001"
      - key: JWT_SECRET
        generateValue: true
      - key: AWS_REGION
        value: us-east-1
      - key: S3_BUCKET_NAME
        value: coachline-audio
      - key: BEDROCK_REGION
        value: us-east-1
      - key: WEB_URL
        sync: false

  - type: web
    name: coachline-web
    runtime: node
    region: oregon
    plan: free
    buildCommand: npm ci && cd apps/web && npm run build
    startCommand: cd apps/web && npm start
    envVars:
      - key: NEXT_PUBLIC_API_URL
        sync: false
      - key: NODE_ENV
        value: production

  - type: redis
    name: coachline-redis
    plan: free
    region: oregon
    maxmemoryPolicy: noeviction

databases:
  - name: coachline-db
    plan: free
    region: oregon
    postgresMajorVersion: "16"
